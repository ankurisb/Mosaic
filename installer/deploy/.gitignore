# These are build-time copies of repo files (compose + the host-path files it
# bind-mounts), refreshed by build-mac.sh / build-win.bat. Not committed — the
# repo root is the single source of truth, avoiding drift.
docker-compose.yml
docker/
NETWORK.md
.env
